{
  "name": "google-web-tests",
  "version": "1.0.0",
  "description": "Web Tests using Cypress and Cucumber frameworks",
  "main": "index.js",
  "scripts": {
    "cypress:runner": "npx cypress open",
    "cypress:execution": "npx cypress run"
  },
  "keywords": [
    "cypress"
  ],
  "author": "Francisco Guerrero",
  "license": "ISC",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^15.0.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.1.5",
    "cypress": "^12.0.2",
    "esbuild": "^0.17.0"
  }
}
